#pragma hdrstop
#pragma argsused

#include <stdio.h>

#ifdef _WIN64
#include <tchar.h>
#else
  typedef char _TCHAR;
  #define _tmain main
#endif

int main (int arg, char ** argv)
{
		int a,b,somme=0,;
		printf("Entre les 2 nombre à multiplier \n")

scanf("%d%d", a, b);
for(i=0;i<b;i+i)
{
	somme=somme * a;
}
printf ("%d * %d=")
}
